server:
  port: 8888
spring:
  application:
    name: config.server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/eduardowmu/microservice_erudio_3
          default-label: main
          search-paths:
            - 'greeting-service*'